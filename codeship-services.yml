builder:
  build:
    dockerfile: ./deployment/Dockerfile.build
    context: ./

publisher:
  encrypted_env_file: deployment/tokens.crypt
  build:
    dockerfile: ./deployment/Dockerfile.build
    context: ./